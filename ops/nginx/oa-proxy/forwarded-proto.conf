# 只接受已确认的直接上游代理提供的原始协议。
geo $remote_addr $oa_trusted_proxy {
    default 0;
    include /etc/nginx/oa-proxy/trusted-proxies.conf;
}

map "$oa_trusted_proxy:$http_x_forwarded_proto" $oa_forwarded_proto {
    default $scheme;
    "1:https" https;
    "1:http" http;
}
