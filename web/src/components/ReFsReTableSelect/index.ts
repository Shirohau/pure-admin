import reFsReTableSelect from "./src/index.vue";
import { withInstall } from "@pureadmin/utils";

/** 表格下拉选择组件（fs-crud 表格内嵌 select 面板） */
export const ReFsReTableSelect = withInstall(reFsReTableSelect);

export default ReFsReTableSelect;
